# API Schema Notes

`schema.sql` is the current source of truth for the initial Supabase CRM schema.

It defines:

- `clients`
- `client_activity`
- `users_roles`
- `tags`
- `client_tags`

Apply it in the Supabase SQL editor or move it into your preferred migration workflow before productionizing the backend.
